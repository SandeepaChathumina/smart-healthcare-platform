apiVersion: apps/v1
kind: Deployment
metadata:
  name: appointment-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: appointment-service
  template:
    metadata:
      labels:
        app: appointment-service
    spec:
      containers:
        - name: appointment-service
          image: docker-appointment-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 5004
          envFrom:
            - secretRef:
                name: appointment-service-secret